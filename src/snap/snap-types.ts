import type { Point } from '@/types';
export type { Point };
export type {
  SnapType,
  AxisLock,
  BezierSegment,
  EdgeInfo,
  AdvancedHitArea,
  CADElement,
  CustomGuideline,
  SnapConstraint,
  SnapAccumulatorState,
  SnapGuideline,
  SnapResultType,
} from '@/types';

export type BoundingBox = import('@/types/snap').BoundingBox;
export type Camera = import('@/types/snap').Camera;
export type SnapConfig = import('@/types/snap').SnapConfig;
export type SnapResult = import('@/types/snap').SnapResult;
